{
  "name": "hot-press",
  "version": "1.0.0",
  "description": "PubSub library with extra asynchronous handling",
  "main": "hot-press.js",
  "scripts": {
    "build": "browserify hot-press.js -o hot-press.browser.js -t [ babelify --presets [ es2015 ] ]",
    "cover": "cat ./coverage/lcov.info | coveralls && rm -rf ./coverage",
    "lint": "eslint hot-press.js test.js",
    "lint-w": "nodemon -x 'npm run lint'",
    "prepublish": "npm test && npm run build",
    "spec": "istanbul cover _mocha --report lcovonly -- --ui tdd --check-leaks",
    "spec-w": "mocha --ui tdd --check-leaks --watch --growl --reporter dot",
    "test": "npm run lint && npm run spec"
  },
  "author": "John Wright <johngeorge.wright@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "babel-preset-es2015": "^6.9.0",
    "babelify": "^7.3.0",
    "browserify": "^13.0.1",
    "chai": "^3.5.0",
    "coveralls": "^2.11.9",
    "eslint": "^2.10.2",
    "istanbul": "^0.4.3",
    "mocha": "^2.4.5",
    "mocha-lcov-reporter": "^1.2.0",
    "sinon": "^1.17.4",
    "sinon-chai": "^2.8.0"
  },
  "dependencies": {
    "lodash.flatten": "^4.2.0"
  }
}
