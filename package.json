{
  "name": "hot-press",
  "version": "2.0.0",
  "description": "Event lifecycle management",
  "main": "hot-press.js",
  "scripts": {
    "build": "babel hot-press.src.js --out-file hot-press.js --presets latest",
    "build-w": "nodemon -i hot-press.js -x 'npm run spec && npm run build'",
    "cover": "cat ./coverage/lcov.info | coveralls && rm -rf ./coverage",
    "lint": "eslint hot-press.src.js test.js",
    "lint-w": "nodemon -x 'npm run lint' -w hot-press.src.js -w test.js",
    "prepare": "npm test && npm run build",
    "spec": "istanbul cover _mocha -- --ui tdd --check-leaks",
    "spec-w": "mocha --ui tdd --check-leaks --watch --growl --reporter nyan",
    "test": "npm run lint && istanbul cover _mocha --report lcovonly -- --ui tdd --check-leaks"
  },
  "keywords": [
    "event",
    "pubsub",
    "broker"
  ],
  "author": "John Wright <johngeorge.wright@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-latest": "^6.16.0",
    "chai": "^4.0.2",
    "commitizen": "^2.9.6",
    "coveralls": "^3.0.0",
    "cz-conventional-changelog": "^2.0.0",
    "eslint": "^4.4.1",
    "eslint-config-standard": "^10.2.1",
    "eslint-plugin-import": "^2.7.0",
    "eslint-plugin-node": "^5.1.1",
    "eslint-plugin-promise": "^3.5.0",
    "eslint-plugin-standard": "^3.0.1",
    "istanbul": "^0.4.5",
    "lodash.functions": "^4.3.0",
    "mocha": "^3.5.0",
    "mocha-lcov-reporter": "^1.2.0",
    "sinon": "^4.0.0",
    "sinon-chai": "^2.12.0"
  },
  "dependencies": {},
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
