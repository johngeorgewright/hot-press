{
  "name": "hot-press",
  "version": "0.0.3",
  "description": "PubSub library with extra asynchronous handling",
  "main": "hot-press.js",
  "scripts": {
    "lint": "eslint hot-press.js test.js",
    "lint-w": "nodemon -x 'npm run lint'",
    "prepublish": "npm test",
    "spec": "istanbul cover _mocha -- --ui tdd --check-leaks",
    "spec-w": "npm run cover -- --watch --growl --reporter dot",
    "test": "npm run lint && npm run spec"
  },
  "author": "John Wright <johngeorge.wright@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "chai": "^3.5.0",
    "eslint": "^2.10.2",
    "istanbul": "^0.4.3",
    "mocha": "^2.4.5",
    "nodemon": "^1.9.2",
    "sinon": "^1.17.4",
    "sinon-chai": "^2.8.0"
  }
}
